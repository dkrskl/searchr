import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: '25%',
        height: 90,
        marginTop: 20,
        padding: 4,
        alignItems: 'center',
    },
    photo: {
        width: '100%',
        height: '100%',
        resizeMode: 'center',
    },
});
